# --- APP / JWT
spring.application.name=2tdspm-api-elysia
api.security.token.secret=elysia-super-secret-key-2025-should-be-long-enough!

# --- ORACLE
spring.datasource.url=jdbc:oracle:thin:@//oracle.fiap.com.br:1521/ORCL
spring.datasource.username=rm557728
spring.datasource.password=monsterhigh
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- JPA (SEM criar tabela automaticamente ? quem cria é o Flyway)
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=none

# --- Flyway (ATIVO)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=true
# Use isto se o schema JÁ tiver tabelas criadas manualmente e ainda não existir flyway_schema_history
# spring.flyway.baseline-on-migrate=true

# --- Swagger fora da raiz
springdoc.swagger-ui.path=/docs
springdoc.swagger-ui.use-root-path=false
