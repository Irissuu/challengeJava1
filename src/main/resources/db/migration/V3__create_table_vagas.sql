CREATE TABLE VAGA_JAVA (
   ID      NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
   STATUS  VARCHAR2(50 CHAR),
   NUMERO  NUMBER(10)         NOT NULL,
   PATIO   VARCHAR2(100 CHAR) NOT NULL
);
